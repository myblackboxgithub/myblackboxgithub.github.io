<Page
    x:Class="MyActivities.Location"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyActivities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="My BlackBox" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot">
            <Image Grid.RowSpan="5" Source="Assets/background-color.jpg" Margin="0 0 0 -100" Opacity="0.3" Stretch="Fill"></Image>

            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock TextWrapping="NoWrap" TextAlignment="Center" Grid.Row="0" Text="Location" FontSize="30" Margin="8,0,0,16" />
                <maps:MapControl x:ConnectionId='1' x:Name="Map" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Height="400" Width="390"                                   />
                <Slider x:ConnectionId='2' x:Name="mySlider" Grid.Row="2" Maximum="30" Minimum="10"                                    />
                <!--<Button x:Name="btnlocation" Grid.Row="3" Content="Get Location" HorizontalAlignment="Center"  Click="btnlocation_Click"/>-->
            </Grid>
        </Grid>
    </Grid>
</Page>

